package com.home.zabara.playground.dsa;

/**
 * Task E8 — Number of islands.
 *
 * `grid` is a 2D array of '1' (land) and '0' (water). TODO: return the
 * number of islands — a group of '1's connected horizontally or vertically
 * (not diagonally). O(rows * cols) time using BFS or DFS flood-fill from
 * every unvisited land cell; mark visited cells however you like (e.g.
 * "sinking" visited land to '0' as you go is the simplest approach and
 * needs no extra visited-set).
 */
public class Task28NumberOfIslands {

    public int numIslands(char[][] grid) {
        throw new UnsupportedOperationException("TODO: BFS/DFS flood-fill, O(rows*cols) time");
    }
}
