package com.home.zabara.playground.dsa;

/**
 * Task E4 — Linked list: reverse + cycle detection.
 *
 * TODO reverse(head): reverse the list in place and return the new head.
 * O(n) time, O(1) extra space — do it iteratively (a recursive solution
 * uses O(n) stack space, which defeats the point).
 *
 * TODO hasCycle(head): detect whether the list has a cycle. O(n) time,
 * O(1) extra space, using Floyd's tortoise-and-hare (a HashSet of visited
 * nodes works but is O(n) space — that's not the point of this exercise).
 */
public class Task24LinkedList {

    public Task24ListNode reverse(Task24ListNode head) {
        throw new UnsupportedOperationException("TODO: iterative reversal, O(1) extra space");
    }

    public boolean hasCycle(Task24ListNode head) {
        throw new UnsupportedOperationException("TODO: Floyd's tortoise and hare, O(1) extra space");
    }
}
