package com.home.zabara.playground.dsa;

/**
 * Task E2 — Longest substring without repeating characters.
 *
 * TODO: implement in O(n) time using a sliding window (two pointers) plus a
 * map/set of character -> last-seen index — not a brute-force check of
 * every substring (O(n^2) or worse). The test enforces this with a large
 * input and a time budget.
 */
public class Task22LongestSubstring {

    public int lengthOfLongestSubstring(String s) {
        throw new UnsupportedOperationException("TODO: sliding window, O(n) time");
    }
}
