package com.home.zabara.playground.dsa;

/**
 * Task E1 — Two Sum.
 *
 * Given an array of integers and a target, return the indices of the two
 * numbers that add up to target (assume exactly one solution, and you may
 * not use the same element twice).
 *
 * TODO: implement in O(n) time using a single pass + HashMap (value -> index)
 * — not the brute-force O(n^2) nested loop. The test enforces this with a
 * large input and a time budget.
 */
public class Task21TwoSum {

    public int[] twoSum(int[] nums, int target) {
        throw new UnsupportedOperationException("TODO: single-pass HashMap lookup, O(n) time");
    }
}
