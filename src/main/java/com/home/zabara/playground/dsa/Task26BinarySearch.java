package com.home.zabara.playground.dsa;

/**
 * Task E6 — Binary search in a rotated sorted array.
 *
 * The array was originally sorted ascending, then rotated at an unknown
 * pivot (e.g. [4,5,6,7,0,1,2], rotated at index 4). All values are unique.
 *
 * TODO: find `target`'s index, or -1 if absent, in O(log n) time — at each
 * step, figure out which half of the current range is still sorted, and use
 * that to decide which half to keep searching in (do not fall back to a
 * linear scan).
 */
public class Task26BinarySearch {

    public int search(int[] nums, int target) {
        throw new UnsupportedOperationException("TODO: modified binary search, O(log n) time");
    }
}
